# Review#499 : NOTICE file is missing
{
  "name": "export_ts",
  "label": "Export TS",
  "description": "Exports Timeseries to a set CSV files in a specified directory tree",
  "family": "Dataset Preparation__Import Export",
  "entry_point": "export_ts.export_ts::export_ts",
  "inputs": [
    {
      "name": "dsname",
      "label": "DSname",
      "description": "The name of the dataset to export",
      "type": "ds_name"
    }
  ],
  "parameters": [
    {
      "name": "pattern",
# Review#499 : be more explicit on how to build this pattern (add an example with metadata) and what data it is composed with and where user can find and name these data in the pattern (metadata ?)
      "description": "pattern used to build the destination tree",
      "type": "text",
      "label": "Destination tree pattern",
      "default_value": "{DSname}/{fid}.csv"
    }
  ],
  "outputs": [
    {
      "name": "path",
      "label": "Path",
      "description": "The path where the export has been generated",
      "type": "text"
    }
  ]
}